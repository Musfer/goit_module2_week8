# goit_module2_week8

Set virtual environment using `pipenv`:
```
pipenv install
pipenv shell
```

Optional:
  1. recreate database 
  ```
  python create_db.py 
  ```
  2. fill database with fake data
  ```
  python fill_data.py
  ```
Hometask:  
  ```
  python main.py
  ```

